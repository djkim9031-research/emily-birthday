# assets

Drop the real photo of Emily here as `emily.jpg`.

- Keep the **filename** `emily.jpg` so `index.html` doesn't need to change.
- Square aspect ratio works best (the page renders it in a ~280–320px frame).
- 600×600 or larger keeps it crisp on high-DPI screens.

The current `emily.jpg` is a friendly pastel placeholder; overwriting it with the real photo is the only change needed.
